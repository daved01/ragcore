# Mock config. Valid file without llm part. See other files for llms.

database:
  provider: "chroma"
  number_search_results: 5
  # For local databases
  base_dir: "data/database"
  document_base_path: "data"

splitter:
  chunk_overlap: 256
  chunk_size: 1024

embedding:
  provider: "openai"
  model: "text-embedding-ada-002"
